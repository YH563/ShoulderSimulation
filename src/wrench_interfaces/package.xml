<?xml version="1.0"?>
<?xml-model href="http://download.ros.org/schema/package_format3.xsd" schematypens="http://www.w3.org/2001/XMLSchema"?>
<package format="3">
  <name>wrench_interfaces</name>
  <version>0.0.0</version>
  <description>ROS2 WrenchData service interface package</description>
  <maintainer email="1066948247@qq.com">yh_ros2</maintainer>
  <license>Apache-2.0</license>  <!-- 补全许可证，避免警告 -->

  <!-- 构建工具依赖（必须） -->
  <buildtool_depend>ament_cmake</buildtool_depend>

  <!-- 接口生成依赖（规范声明） -->
  <build_depend>rosidl_default_generators</build_depend>
  <exec_depend>rosidl_default_runtime</exec_depend>
  <member_of_group>rosidl_interface_packages</member_of_group>
  
  <!-- builtin_interfaces依赖（修复IDE报错的核心） -->
  <depend>builtin_interfaces</depend>

  <!-- 测试依赖 -->
  <test_depend>ament_lint_auto</test_depend>
  <test_depend>ament_lint_common</test_depend>

  <!-- 移除冗余的export标签（ament_cmake是默认构建类型，无需手动声明） -->
</package>